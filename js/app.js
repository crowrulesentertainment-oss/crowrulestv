window.CRP={};CRP.card=x=>{let p=new URLSearchParams({type:x.type,id:x.youtubeId||x.id,title:x.title});return `<article class="card"><a href="watch.html?${p}"><div class="thumb">${x.thumbnail?`<img src="${x.thumbnail}" alt="">`:''}<span class="badge ${x.live?'live-badge':''}">${x.live?'● LIVE':x.source.toUpperCase()}</span></div><div class="card-body"><h3>${x.title}</h3><p>${x.description}</p></div></a></article>`};CRP.render=(e,a)=>{if(e)e.innerHTML=a.map(CRP.card).join('')||'<p>No titles found.</p>'};CRP.initBrowse=()=>{let e=document.querySelector('#browseGrid');CRP.render(e,CRP_DATA);document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');let f=b.dataset.filter;CRP.render(e,f==='all'?CRP_DATA:f==='live'?CRP_DATA.filter(x=>x.live):f==='original'?CRP_DATA.filter(x=>x.original):CRP_DATA.filter(x=>x.source===f))})};CRP.initLive=()=>CRP.render(document.querySelector('#livePageGrid'),CRP_DATA.filter(x=>x.live));CRP.initMyList=()=>{let ids=JSON.parse(localStorage.getItem('crp_my_list')||'[]');CRP.render(document.querySelector('#myListGrid'),CRP_DATA.filter(x=>ids.includes(x.id)))};CRP.initSearch=()=>{let f=document.querySelector('#searchForm'),i=document.querySelector('#searchInput'),g=document.querySelector('#searchGrid');f.onsubmit=e=>{e.preventDefault();let q=i.value.toLowerCase();CRP.render(g,CRP_DATA.filter(x=>(x.title+' '+x.description+' '+x.category).toLowerCase().includes(q)))}};document.addEventListener('DOMContentLoaded',()=>{CRP.render(document.querySelector('#liveGrid'),CRP_DATA.filter(x=>x.live));CRP.render(document.querySelector('#originalGrid'),CRP_DATA.filter(x=>x.original))});
